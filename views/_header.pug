header.header
  nav.nav.nav--tours
    a.nav__el(href='#') Pars Air
  .header__logo
    img(src='/img/parsLogo.png' alt='ParsLgo logo')
  nav.nav.nav--user
    if user
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/verifyPhoneNumber') Log in
      //- a.nav__el.nav__el--cta(href='/#') Sign up